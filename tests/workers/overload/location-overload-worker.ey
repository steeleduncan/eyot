// this tests we can gpu/cpu overload at the point of worker creation

cpu fn mult(val i64) i64 {
   return val * 2
}

gpu fn mult(val i64) i64 {
   return val * 3
}

cpu fn main() {
    let c = cpu mult
    send(c, [i64]{ 2, 3, 4 })
    for v: drain(c) {
        print_ln("cpu: ", v)
    }

    let g = gpu mult
    send(g, [i64]{ 2, 3, 4 })
    for v: drain(g) {
        print_ln("gpu: ", v)
    }
}

